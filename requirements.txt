langgraph==1.1.6
langchain==1.2.15
openai==2.30.0
anthropic==0.97.0
pyyaml==6.0.3
cfn-lint==1.48.1
checkov==3.2.513
python-dotenv==1.2.2
yamllint==1.38.0
# checkov (through at least 3.3.16, the latest release as of writing) hard-pins
# boto3==1.35.49 in its own package metadata, and that boto3 version requires
# botocore<1.36.0 — which predates CloudFormation's DescribeEvents operation
# (used by tools/deploy_validator.py to enrich "early validation" deploy
# failures; degrades gracefully to today's behavior when unavailable). No
# boto3 version satisfies both constraints, so `pip install -r
# requirements.txt` intentionally keeps boto3 at what checkov demands here.
# To enable that enrichment, run this AFTER installing requirements.txt:
#   pip install --upgrade boto3==1.43.90
# (checkov's own runtime behavior is unaffected by the newer boto3 — verified
# by running it against a test template with 1.43.90 installed.)
boto3==1.35.49
requests==2.33.1
sentence_transformers==5.3.0
numpy==2.4.4
faiss-cpu==1.12.0
rank_bm25==0.2.2
sentence_transformers==5.3.0
httpx==0.28.1
pandas==3.0.2
neo4j==6.1.0
chromadb==1.5.8
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
langchain-core==1.3.2
langchain-ollama==1.1.0
datasets==4.8.5
beautifulsoup4==4.14.3
terraform-local==0.26.0